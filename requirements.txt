# 0.2.20 pins transformers<4.29 (incompatible with Llama 3 / GQA). 0.2.36 does not;
# we register llama-3 in utils/llama3_conv.py when missing from stock FastChat.
fschat>=0.2.36
nltk==3.8.1
# NumPy 2.x breaks torch 2.1.x and common CC wheels (e.g. gradio 3.50); keep 1.x.
numpy>=1.25,<2
openai==0.28.1
torch>=2.1,<2.2
tqdm==4.66.1
# Llama 3 uses GQA (num_key_value_heads < num_attention_heads). Older
# transformers (e.g. 4.28) builds wrong k/v shapes and fails loading with
# e.g. "Trying to set a tensor of shape [1024, 4096] ... [4096, 4096]".
# PyTorch 2.1.x (common on Compute Canada): use transformers 4.40–4.45.
# - transformers 5.x expects torch >= 2.4 (see earlier notes).
# - transformers 4.46+ often calls torch.utils._pytree.register_pytree_node,
#   which does not exist on torch 2.1 (only _register_pytree_node); import fails.
transformers>=4.40.0,<4.46.0
sentencepiece==0.1.99
protobuf==4.24.4
accelerate>=0.27.0
pandas>=2.1.0
